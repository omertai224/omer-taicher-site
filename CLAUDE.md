# כללי ברזל לפרויקט

## דחיפות תכופות — הכלל הכי חשוב בפרויקט!
**אל תחכה שעומר יגיד לך לדחוף. תדחוף מעצמך.**
- **כל שינוי משמעותי = commit + push מיד.** לא אחרי שגומרים — אחרי כל שלב.
- **סבב ניתוח? דחיפה. שינוי HTML? דחיפה. עדכון קובץ? דחיפה.**
- **למה?** כי סשנים נתקעים. אייג'נטים נופלים. דברים נשברים באמצע. בלי דחיפות — מתחילים מהתחלה. וזה מתיש.
- **עדיף 10 דחיפות קטנות מדחיפה אחת גדולה שלא מגיעה.**
- **אם נתקעים — ממשיכים מאיפה שדחפנו.** הסשן הבא קורא מה שנדחף וממשיך. אפס מאמץ כפול.
- **זה לא רק בהדרכות — זה בכל עבודה.** קוד, תוכן, ניתוח, כל דבר.

## גופן Rubik - לא לגעת!
הגופן Rubik חייב להיטען **תמיד**, בכל מצב, בלי תנאים.
- לא משנה כמה זמן לוקח לו להיטען
- לא משנה איזה מכשיר או חיבור אינטרנט
- לא משנה מה הציון ב-PageSpeed
- אסור להחליף אותו ב-`display:optional` או להתנות את הטעינה שלו
- Rubik הוא הגופן של האתר, תמיד נטען, נקודה.

## תהליכי עבודה - הדבר הכי חשוב!
**בתחילת כל סשן חדש — קרא את תהליכי העבודה לפני כל דבר אחר.**
ככה אתה יודע בדיוק מה לעשות בלי שעומר צריך להסביר.

תהליכי העבודה נמצאים בתיקייה `pages/workflow/` ומחולקים לקבצים קטנים:
- **`workflow.css`** — עיצוב (כמעט אף פעם לא נוגעים)
- **`workflow.js`** — לוגיקת טעינת טאבים ותתי-לשוניות
- **`index.html`** — השלד (nav, tabs, footer)
- **`workflow.html`** — תהליך עבודה כללי, כללים, מוקשים ולקחים
- **`posts.html`** — נוסחת-על לפוסטים
- **`interactive.html`** — נוסחת-על להדרכות
- **`payments.html`** — מוצרים ותשלומים
- **`sitemap.html`** — מבנה האתר
- **`ideas.html`** — טאב "רעיונות להמשך" (שלד עם תתי-לשוניות)
- **`ideas/`** — תיקייה עם תתי-לשוניות: priorities, blog-ideas, interactive-ideas, products, sales, site, marketing
- **`calls.html`** — ניתוח שיחות לקוחות

### ניתוח תמלולים — כלל ברזל!
- **שני ריפוס במקביל:** `Calls` (פרטי — תמלולים ב-`inbox/` על main, catalog.json, analysis_log.json) + `omer-taicher-site` (רעיונות מזוקקים בתיקיית `ideas/`)
- **תמלולים על main:** כל התמלולים יושבים על בראנצ׳ `main` בריפו `omertai224/Calls`. Claude משתכפל מ-main ומושך הכל. תמלולים לא נמחקים.
- **analysis_log.json = מקור האמת** — קורא אותו ראשון כדי לדעת מה כבר נותח. תמלולים מתחת ל-200 מילים = קצר מדי, לדלג
- **תובנות מחולקות לקבצים קטנים** ב-`insights/categories/` (8 קבצים לפי נושא). **לא לקרוא את כולם בבת אחת!** קרא `insights/index.json` ואז רק את הקובץ הרלוונטי
- **שיטת ניתוח מוכחת — מגה-סבבים (עדכון מרץ 2026):**
  - **הגישה:** 5 אייג'נטים במקביל, כל אחד מנתח כמה שיחות. גודל הבאצ' לפי אורך הקבצים:
    - קבצים קצרים (עד 200 שורות): **10 קבצים לאייג'נט** = 50 בסבב
    - קבצים בינוניים (200-500 שורות): **5 קבצים לאייג'נט** = 25 בסבב
    - קבצים ארוכים (500+ שורות): לאייג'נט לקרוא רק 300 שורות ראשונות, ניתוח חלקי
    - קבצים ענקיים (1000+ שורות): אייג'נט בודד לקובץ בודד, בזהירות
  - **פורמט:** כל אייג'נט מחזיר JSON array עם אובייקטים: `file, client, lines, quality (1-5), quality_notes`
  - **שמירה:** אסוף תוצאות מכל האייג'נטים, הוסף ל-analysis_log.json בבת אחת (לולאת Python), commit + push
  - **דילוג בבאלק:** קבצים מתחת ל-200 מילים — לדלג על כולם בפעולה אחת, `status: "skipped"`
  - **אל תשאל את עומר, פשוט תמשיך!**
- **ביקורת אחרי כל גל גדול (חובה!):**
  1. כיסוי מלא — אין קבצים חסרים (inbox - analysis_log = 0)
  2. אין כפילויות ב-analysis_log
  3. אין דילוגים בטעות — קבצים 200+ מילים שסומנו skipped
  4. כל רשומה analyzed יש לה client, quality, quality_notes
  5. JSON תקין (json.dumps לא נופל)
  6. local = remote (git fetch + compare HEAD)
  7. working tree נקי (git status clean)
- **עבוד בחתיכות קטנות — דחוף אחרי כל סבב!** תמיד דחוף לשני הריפוזיטוריות
- **כל 5 סבבים (25 שיחות) — עדכן את האתר!** אל תצבור עשרות שיחות בלי לעדכן ideas/ באתר. כל 5 סבבים: בדוק מה חדש בתובנות, הוסף רעיונות חדשים לאתר, ודחוף. אם עומר מבקש לסדר באתר — עשה מיד
- **לא לגעת באתר!** ניתוח תמלולים הולך רק ל-"רעיונות להמשך" (תיקיית `ideas/`). לא משנים דפים, לא מוסיפים תוכן לאתר, לא נוגעים בקוד — עד שעומר נותן אישור מפורש

### סדר עדיפויות — מה שעושה כסף קודם!
1. **הדרכות אינטראקטיביות** — המקור המרכזי לכסף! 27-97 ש"ח, חטיפים. כל בעיה שחוזרת = רעיון להדרכה. **זה החלק המאתגר** — דורש בניית מוצר, לא רק כתיבה.
2. **מוצרים ושירותים** — חבילות, ריטיינר, שירותי פרימיום. עושים כסף ישירות.
3. **פוסטים לבלוג** — **עומר עושה בצ'יפס!** פוסטים קלים לו, לא צריך להתמקד בהם. הם מביאים תנועה ומובילים להדרכות.
4. **שיפורי מכירות, אתר, שיווק**
- **הכל חשוב!** הכל מערכת אחת שמזינה אחד את השני. הדרכות אינטראקטיביות הן הלב — כל השאר תומך, מוביל, ומזין אותן. שום דבר לא נזרק או מדולג.
- **עיקרון מנחה:** מה שיותר קל לעומר לייצר = קודם. כמות תוכן > שלמות. עדיף 10 הדרכות "טובות" מ-3 "מושלמות".

### פורמט כל רעיון
- **כותרת** (למשל "איך לסדר סיסמאות")
- **משפט** שמסביר למה זה רלוונטי
- **ציטוט מהשיחה** שמוכיח שהבעיה אמיתית
- **ספירת ביקוש** — כמה פעמים הבעיה עלתה בשיחות שונות
- **תיוג דחיפות** — "ביקוש מוכח" (עלה כמה פעמים) או "רעיון חדש" (עלה פעם אחת)
- **קישור להדרכה קיימת** אם רלוונטי (למשל Vibe)
- מספיק פרקטי שעומר יכול להגיד "תעשה את זה" ולקבל תוצאה מוגמרת
- רעיונות הולכים לתת-הלשונית המתאימה: interactive-ideas (ראשון!), blog-ideas, products, sales, site, marketing

### דירוג איכות (1-5) לכל קובץ
כל קובץ שנותח מקבל דירוג ב-analysis_log: 5=מצוין (נטו עבודה, שווה לחזור), 4=טוב, 3=בינוני, 2=מעט תוכן, 1=לא שימושי. ככה אפשר לחזור לקבצים איכותיים כשצריך לזקק נושא ספציפי.

### בעיית תמלולים פגומים
הרבה תמלולים סובלים מחזרות מאסיביות ("תודה", "כן", "זהו" חוזרות מאות פעמים). התוכן האמיתי הוא לרוב 10%-30% מהקובץ. לכן:
- קבצים עם 1000+ שורות לא בהכרח עשירים — יכולים להיות 90% רעש
- כשקוראים קובץ ארוך, להתחיל מ-300 שורות ראשונות ולהעריך
- אם התמלול פגום, לתת quality 1-2 ולהמשיך הלאה

### מה לדלג עליו
- **שיחות אישיות / לא טכניות** — לפי שיקול דעת. לסמן בלוג שדולגה ולהמשיך
- **תמלולים מתחת ל-200 מילים** — קצר מדי. לסמן בלוג
- **השיחות עצמן הן מקור האמת** — לא להניח כלום מראש. לקרוא ולתת לתובנות לעלות מהתמלול

### עדכון האתר — בדיקת כיסוי נושאים (עדכון מרץ 2026)
כשמעדכנים את האתר, לא מספיק להוסיף רעיונות בודדים. צריך לבדוק **כיסוי נושאים**:
1. חלץ את כל הנושאים מ-analysis_log (ספור keywords ב-quality_notes)
2. השווה מול כותרות באתר — אילו נושאים חסרים?
3. הוסף רעיונות לנושאים חסרים
4. בדוק כפילויות כותרות (Counter על כל הכותרות)
5. וולידציית HTML: cards == headers == bodies == titles, divs open == close

### דף עדיפויות (priorities.html)
הדף הזה מכיל את סדר העדיפויות מבוסס נתונים:
- **שכבה 1:** WIN מהיר — ביקוש מטורף + קל לייצר (סיסמאות 31x, גיבוי 77x, אנטיוירוס 52x)
- **שכבה 2:** הבשר — ביקוש חזק + ערך גבוה (WiFi 39x, Outlook 32x, Office 43x)
- **שכבה 3:** הרחבה — ביקוש בינוני (Zoom 30x, WhatsApp 22x, PDF 20x)
- **מוצרים חדשים, שיפורי מכירות, שיווק**
- לעדכן את העדיפויות כשמצטבר מידע חדש

### catalog.json — מה נשמר
- תובנות לפי קטגוריות דינמיות (קטגוריות חדשות נולדות מהתמלולים)
- **פרופילי לקוחות** — מידע כללי בלבד: גיל, מקצוע, כמה זמן לקוח, העדפות טכנולוגיות. בלי פרטים רגישים!

### סטטוס ניתוח שיחות (עדכון מרץ 2026)
- **657/657 קבצים עברו ניתוח** — 539 נותחו, 118 דולגו (קצרים מדי)
- **83 רעיונות באתר** — 47 הדרכות, 27 בלוג, 9 מוצרים
- **21 נושאים מכוסים** — כל הנושאים שעלו 3+ פעמים יש להם כרטיס באתר
- **דף עדיפויות מלא** — שכבות 1-2-3 + מוצרים + שיווק
- **7 כרטיסים במכירות (sales)** — טיעוני מכירה + אפסייל + חבילות משפחתיות
- **אם מתווספות שיחות חדשות:** לנתח רק את החדשות (analysis_log כבר מכסה את כל הישנות)

### כללים נוספים
- **כל החלטה וסיכום בשיחה עם עומר = לעדכן מיד בתהליכי עבודה.** לא לחכות. לא לשכוח. זה הזיכרון בין סשנים
- **שום מידע אישי של לקוחות לא עובר ל-omer-taicher-site** — רק רעיונות נקיים

### עדכון שוטף
- **כל פעם שלומדים משהו חדש** במהלך עבודה (טכניקה, מוקש, שיטה טובה) - לעדכן את הקובץ המתאים
- **בסוף כל סשן** - לבדוק אם יש משהו חדש שכדאי לתעד בתהליכי העבודה
- זה כולל: באגים שחוזרים, פתרונות שעובדים, שיטות עבודה חדשות
- הקבצים האלה הם הזיכרון בין סשנים - מה שכתוב שם ישרת סשנים עתידיים

## שדרוג הדרכות אינטראקטיביות — מגולמי למושלם (עדכון מרץ 2026)
**המטרה:** עומר אומר "תשדרג לי את X" — והתוצאה מושלמת בלי שאלות.
**הדרכת הייחוס:** `interactive/tutorials/Everything/` — להעתיק משם style.css ו-script.js.

### כלל הברזל: דחיפות = ביטוח חיים!
- **דחיפה אחרי כל שלב.** לא אחרי שגומרים — אחרי כל פעולה.
- **אם נתקעים — ממשיכים מאיפה שדחפנו.** אפס מאמץ כפול.

### תהליך שדרוג אוטומטי — "תשדרג לי את X"
כשעומר נותן הדרכה גולמית, הסדר הוא:

#### שלב 1: העתקת תבנית
```bash
# להעתיק style.css ו-script.js מ-Everything לתיקיית ההדרכה החדשה
cp interactive/tutorials/Everything/style.css interactive/tutorials/[שם]/style.css
cp interactive/tutorials/Everything/script.js interactive/tutorials/[שם]/script.js
# להעתיק תמונות ניווט
cp interactive/tutorials/Everything/images/right.png interactive/tutorials/[שם]/images/
cp interactive/tutorials/Everything/images/left.png interactive/tutorials/[שם]/images/
cp interactive/tutorials/Everything/images/right-disabled.png interactive/tutorials/[שם]/images/
cp interactive/tutorials/Everything/images/left-disabled.png interactive/tutorials/[שם]/images/
cp interactive/tutorials/Everything/images/logo.png interactive/tutorials/[שם]/images/
```
**דחיפה!**

#### שלב 2: ניתוח תמונות (השלב הכבד)
- **5 אייג'נטים במקביל**, כל אחד מנתח 2 תמונות (סבבים של 10)
- כל אייג'נט מחזיר JSON: `image_num`, `image_file`, `description_en`, `action_he`, `click_area`, `arrow_direction`
- `action_he` = טקסט עברי עם `<span style="color:#f6a67e;">` להדגשת אלמנט ללחיצה
- שומרים ל-`analysis.json`, **דחיפה אחרי כל סבב!**
- אל תמחק קבצי `.flow` ו-`.fss`

#### שלב 3: עדכון HTML — 5 פעולות מדויקות
בסדר הזה, על `index.html` של ההדרכה:

**פעולה 1 — החלפת טקסט לעברית:**
- סקריפט Python שלוקח מ-analysis.json ומחליף את הטקסט האנגלי הגולמי
- כל בועת טקסט: `text-align: right`, `dir="rtl"`

**פעולה 2 — תיקון גובה כל תמונה:**
```python
# Replace ALL image heights to match 80px nav bar
# חיפוש: height:calc(100vh - XXpx)  (כל ערך ישן)
# החלפה ב:
"height:calc(100vh - 80px)"
```

**פעולה 3 — תיקון מונה צעדים:**
```python
# לספור כמה צעדים יש (שקפים - 3 פתיחה - 1 סיום)
num_steps = total_slides - 4
# להחליף את המונה בכל בועה:
# מ: /XX  (כל מספר ישן)
# ל: /num_steps
```

**פעולה 4 — הוספת 3 שקפי פתיחה:**
לפני שקף הצעד הראשון, להוסיף (בסדר הזה):
1. **שקף פתיחה** — העתקה מ-Everything, שינוי שם תוכנה + תיאור
2. **שקף סרטון** — "צפו בסרטון איך עובדים עם ההדרכה" (העתקה מ-Everything)
3. **שקף הסבר** — placeholder (העתקה מ-Everything, שינוי שם תוכנה)

**פעולה 5 — לוגו:**
```html
<img src=".//images//logo.png" style="z-index:3;position:fixed;bottom:17px;left:30px;height:46px;">
```

**דחיפה!**

#### שלב 4: בדיקת תקינות (צ'קליסט)
- [ ] מסך פתיחה נטען עם שם ההדרכה
- [ ] סרטון "איך עובדים" מופיע בשקף 2
- [ ] שקף הסבר (placeholder) בשקף 3
- [ ] צעדים ממוספרים מ-1 עד N בבועות
- [ ] פס ניווט: שתי שורות מיושרות, 3 ריקים בהתחלה + 1 ריק בסוף
- [ ] מספרים משמאל לימין (direction: ltr)
- [ ] תמונות לא נחתכות (calc(100vh - 80px))
- [ ] לוגו ממורכז בפס (bottom:17px)
- [ ] מסגרות כתומות פועלות (box-pulse animation)
- [ ] חיצי ניווט עובדים
- [ ] מסך סיום תקין

### מפרט טכני קריטי (קוד מדויק)

#### CSS — הנקודות הקריטיות ב-style.css:
```css
.mySlides { min-height: calc(100vh - 80px); }

.nav-background {
  height: 80px;
  padding: 4px 130px 4px 90px; /* לוגו משמאל, חיצים מימין */
}

.nav-dots {
  display: grid;
  direction: ltr;
  gap: 4px;
  width: 100%;
  height: 100%;
  align-content: center;
  justify-items: center;
}

.nav-dot {
  width: 26px;
  height: 26px;
  border-radius: 50%;
  font-family: 'Rubik', Arial, sans-serif;
  font-size: 11px;
  font-weight: 600;
  color: #fff;
  background-color: #a8c5d6;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  border: none;
  padding: 0;
}

.nav-dot.active { background-color: #1e5f74; }
.nav-dot:hover { background-color: #1e5f74; transform: scale(1.15); }
.prev, .next { bottom: 20px; }
```

#### JS — פונקציית buildNavDots (להעתיק כמו שהיא):
```javascript
function buildNavDots() {
  let slides = document.getElementsByClassName("mySlides");
  let container = document.querySelector('.nav-dots');
  if (!container) return;
  var cols = Math.ceil(slides.length / 2);
  container.style.gridTemplateColumns = 'repeat(' + cols + ', 1fr)';
  for (let i = 0; i < slides.length; i++) {
    let dot = document.createElement('button');
    dot.className = 'nav-dot';
    var skipStart = 3; // 3 שקפי פתיחה בלי מספר
    var skipEnd = 1;   // שקף סיום בלי מספר
    var isNumbered = (i >= skipStart && i < slides.length - skipEnd);
    var stepNum = isNumbered ? (i - skipStart + 1) : '';
    dot.title = isNumbered ? stepNum.toString() : (i < skipStart ? ['פתיחה','סרטון','הסבר'][i] : 'סיום');
    dot.textContent = stepNum.toString();
    (function(index) {
      dot.addEventListener('click', function() { showSlides(index + 1); });
    })(i);
    container.appendChild(dot);
  }
}
```

#### HTML — תבנית בועת טקסט לכל צעד:
```html
<div class="text arrow-right" style="left:calc(X% - 315px);top:calc(Y% - 25px);position:absolute;width:300; height: fit-content;">
  <div style="text-align: right;">1<div style="color: #ffffffbb;display: inline;">/N</div></div>
  <b style="font-size:24px;padding: 8px 0px;"></b>
  <div>טקסט ההוראה עם <span style="color:#f6a67e;">אלמנט ללחיצה</span></div>
</div>
```

#### שקפי מעבר (TODO — עוד לא מיושם)
- בין חלקי ההדרכה (למשל "הורדה" → "התקנה" → "שימוש") צריך שקפי מעבר
- שקף מעבר = עיגול ריק (בלי מספר), עם כותרת של החלק הבא
- עומר יגדיר איפה בדיוק לשים אותם

## כלל קבצים קטנים
- **קובץ גדול וקשה לקריאה = מחלקים אותו.** לא דוחפים הכל לקובץ אחד.
- אם קובץ JSON/HTML גדל מעבר ל-200 שורות — לשקול פיצול לקבצים קטנים לפי נושא.
- איפה שפיצול לא שובר פונקציונליות — לפצל. איפה שכן (כמו פאנל אדמין) — להשאיר.
- קבצים קטנים = סשנים שלא נתקעים.

### קבצים לעקוב אחריהם (עדכון מרץ 2026)
- **analysis_log.json (8,216 שורות, 307KB)** — לא לפצל! זה מקור האמת. אבל **לעולם לא לקרוא את כל הקובץ** — רק דרך Python: `set(a['file'] for a in log['analyses'])`. אם ייכנסו שיחות חדשות, הקובץ ימשיך לגדול — זה OK.
- **interactive-ideas.html (1,676 שורות, 47 כרטיסים)** — אם מגיע ל-60+ כרטיסים, לפצל לפי קטגוריה (מחשב, ענן, טלפון, אבטחה).
- **blog-ideas.html (905 שורות, 27 כרטיסים)** — מתקרב לגבול 1000. לעקוב.
- **כל השאר תקין** — categories/ מפוצל ל-8 קבצים מאוזנים, CLAUDE.md קומפקטי.

## דף הבית (index.html) - CSS אינליין!
דף הבית טוען את `main.css` **אסינכרונית** (`media="print" onload="this.media='all'"`).
- כל CSS לרכיבים דינמיים חדשים חייב להיכתב **בתוך ה-`<style>` האינליין** בראש הדף
- אם שמים CSS רק ב-main.css, הרכיב יופיע **בלי עיצוב** בטעינה הראשונית
- זה לא רלוונטי לדפים אחרים (interactive/index.html כותב CSS אינליין בכל מקרה)
